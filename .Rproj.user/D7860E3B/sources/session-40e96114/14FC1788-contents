#' Volcano plot of the gene set enrichment meta-analysis result
#'
#' It allows to show aa volcano plot of the gene set enrichment meta-analysis
#' result
#'
#'
#' @param resMA Output generated by the  function that performs
#' meta-analysis (metaAnalysisDEpath).
#'
#' @param fdrSig Adjusted p-value from which a gene set is considered 
#' significant. Default 0.05
#'
#' @param comES_Sig In absolute value. Combine effect size
#' threshold from which gene sets are considered
#'
#' @param show_paths_names boolean specifying if significant 
#' genes sets names must be shown.
#'
#'
#'
#'
#' @author Juan Antonio Villatoro Garcia,
#' \email{juanantoniovillatorogarcia@@gmail.com}
#'
#' @seealso \code{\link{createObjectMA}}, \code{\link{metaAnalysisDE}}
#'
#' @examples
#'
#'
#' resultsMA <- metaAnalysisDE(objectMApath_sim, typeMethod="REM")
#' makeHeatmap(objectMA=objectMApath_sim, resultsMA,
#' scaling = "zscor", regulation = "all",breaks=c(-2,2),
#' fdrSig = 0.05, comES_Sig = 1.5, numSig=40)
#'
#' @export

heatmapPaths <- function(
        objectMApath,
    resMA,
    fdrSig = 0.05,
    comES_Sig = 0.5,
    color = colorRampPalette(rev(brewer.pal(n = 7, name = "RdYlBu")))(100),
    na_col = "darkgrey",
    legend = TRUE,
    cluster_cols= FALSE,
    cluster_rows= FALSE,
    show_rownames = TRUE,
    show_colnames = FALSE)
{
    
}
